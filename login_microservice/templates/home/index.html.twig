{# templates/home/index.html.twig #}

{% extends 'base.html.twig' %}

{% block body %}
    <h1>Delivery Planning</h1>
    {{ form_start(form) }}
    {{ form_row(form.deliveryDate) }}
    {{ form_row(form.locations) }}
    <button type="submit">Plan Delivery</button>
    {{ form_end(form) }}

    {# Example: Display a map with hardcoded latitude and longitude #}
    <iframe
        width="100%"
        height="500"
        frameborder="0" style="border:0"
       src="https://maps.google.com/maps?q=Tangesir%20Dates%20Products&amp;t=&amp;z=13&amp;
        ie=UTF8&amp;iwloc=&amp;output=embed" width=300 height=150 allowfullscreen>
   </iframe>
    

{% endblock %}
