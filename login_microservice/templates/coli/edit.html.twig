{# templates/coli/edit.html.twig #}

{% extends 'base.html.twig' %}

{% block title %}Edit a Package{% endblock %}

{% block body %}
<div class="container mt-5">
    <h1>Edit a Package</h1>
    
    {# Display form errors if any #}
    {% if form.vars.errors|length > 0 %}
        <div class="alert alert-danger">
            <strong>Error:</strong> Please correct the errors below.
            <ul>
                {% for error in form.vars.errors %}
                    <li>{{ error.message }}</li>
                {% endfor %}
            </ul>
        </div>
    {% endif %}

    {{ form_start(form) }}
        <div class="mb-3">
            {{ form_row(form.reference, {'label': 'Reference', 'attr': {'class': 'form-control'}}) }}
        </div>
        <div class="mb-3">
            {{ form_row(form.typeDeColi, {'label': 'Package Type', 'attr': {'class': 'form-control'}}) }}
        </div>
        <div class="mb-3">
            {{ form_row(form.categories, {'label': 'Categories', 'attr': {'class': 'form-control'}}) }}
        </div>
        <button class="btn btn-primary">Update</button>
    {{ form_end(form) }}
</div>
{% endblock %}
